import { ModalConfig } from './toast.model';

export const MODAL_CONFIG_DEFAULTS: ModalConfig=
{
    duration: 5000,
    fade: true,
    limit: 5,
    message: 'General Message',
    position: 'top-right'
    
}